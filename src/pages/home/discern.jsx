/* eslint-disable */
import React, { useState, useCallback } from 'react';
import {
  preCls,
} from './constant';

const Discern = (props) => {
  
  return (
    <div className={preCls}>
      test home 
    </div>
  );
};

export default Discern;
